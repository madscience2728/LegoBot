# Dependencies for running on the PC: lego_brain/robot_client.py (requests,
# + lego_control's own pylgbst[bleak] for the direct-BLE fallback path),
# lego_brain/pc_server.py (fastapi, uvicorn), and lego_brain/media_client.py
# (websockets, webrtcvad). numpy is only needed by scripts/pc_media_client.py's
# optional --preview mode (opencv-python-headless too, install separately
# if you want that).
requests
fastapi
uvicorn
websockets
webrtcvad
pylgbst[bleak]
